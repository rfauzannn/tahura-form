// Supabase API integration will be inserted here after you set up Supabase URL and anon key
document.getElementById('tahuraForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    alert('Data akan dikirim ke Supabase setelah integrasi API ditambahkan.');
});
